<a href="#ask_question-{{block.id}}" data-bs-toggle="modal" class="tf-product-extra-icon">
    <div class="icon">
        <i class="{{ block.settings.icon }}"></i>
    </div>
    <div class="text fw-6">{{ block.settings.title }}</div>
</a>

{% render 'contact-modal', id: block.id, title: block.settings.title %}

{% schema %}
    {
        "name": "Ask questions",
        "settings": [
            {
                "type": "select",
                "id": "icon",
                "label": "Icon",
                "options": [
                    {
                        "value": "icon-question",
                        "label": "Question"
                    },
                    {
                        "value": "icon-car",
                        "label": "Truck"
                    },
                    {
                        "value": "icon-share",
                        "label": "Share"
                    },
                    {
                        "value": "icon-compare",
                        "label": "Compare"
                    },
                    {
                        "value": "icon-delivery-time",
                        "label": "Delivery"
                    }
                ],
                "default": "icon-question"
            },
            {
                "type": "inline_richtext",
                "id": "title",
                "label": "Title",
                "default": "Ask a questions"
            }
        ],
        "presets": [
            {
                "name": "Ask questions"
            }
        ]
    }
{% endschema %}
